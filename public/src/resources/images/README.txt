
To convert Inkscape SVG, which uses name spaces, into a simplified form that
JSX can import, use a converter site such as:

  https://svg2jsx.com/

Opted to paste out the SVG manually into a separate file.
